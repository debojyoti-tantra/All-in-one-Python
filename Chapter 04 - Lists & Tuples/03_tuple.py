# we can change list but cann't cahnge tupple

a = (2, 5, 6.37, "rohan", "orange")
print(type(a))   # output: <class 'tuple'>
print(a)   # output: (2, 5, 6.37, 'rohan', 'orange')

# a[0] = 67 (wrong opration) we can't change element of tupple